{
  "hooks": [
    {
      "event": "SessionStart",
      "command": "python3 -c 'import yaml, matplotlib' 2>/dev/null || pip install -q -r ${CLAUDE_PLUGIN_ROOT}/scripts/requirements.txt"
    }
  ]
}
